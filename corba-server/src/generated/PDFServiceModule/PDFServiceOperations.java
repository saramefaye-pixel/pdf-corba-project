package PDFServiceModule;


/**
* PDFServiceModule/PDFServiceOperations.java .
* Generated by the IDL-to-Java compiler (portable), version "3.2"
* from src/main/java/com/pdfcorba/PDFService.idl
* Thursday, May 7, 2026 1:41:51 PM GMT
*/

public interface PDFServiceOperations 
{

  // 1. Fusion de plusieurs PDFs en un seul
  byte[] mergePDFs (byte[][] pdfFiles);

  // 2. Découpage : retourne une liste de PDFs (1 par page)
  byte[][] splitPDF (byte[] pdfFile);

  // 3. Extraction de pages spécifiques (ex: "1,3,5")
  byte[] extractPages (byte[] pdfFile, String pageNumbers);

  // 4. Suppression d'une page (numéro commence à 0)
  byte[] deletePage (byte[] pdfFile, int pageIndex);

  // 5. Ajout d'un mot de passe
  byte[] addPassword (byte[] pdfFile, String password);

  // 6. Conversion PDF -> Images (retourne liste d'images PNG)
  byte[][] convertToImages (byte[] pdfFile);

  // 7. Extraction du texte brut
  String extractText (byte[] pdfFile);

  // 8. Création d'un PDF à partir d'un texte
  byte[] createPDF (String content);
} // interface PDFServiceOperations
